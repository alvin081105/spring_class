server:
  port: 8084

spring:
  application:
    name: api_practice

  datasource:
    url: jdbc:mysql://localhost:3330/spring_class_school?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: root
    password: "1234"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  jwt:
    secret: "THIS_IS_AT_LEAST_32_BYTES_SECRET_KEY_EXAMPLE_1234"

springdoc:
  packages-to-scan: com.kch.api_project.controller
